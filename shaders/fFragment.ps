#version 420

layout(location=0) out vec4 out_color;

//in vec2 uv;

//uniform sampler2D diffuseTexture;
//uniform image2D diffuseTexture;


void main()
{
//	vec4 textColor = texture(diffuseTexture, uv);
	
	
	
	//vec4 textColor = imageLoad(diffuseTexture,uv);
//vec4(uv.x,uv.y,0,1);	
//vec4(0.0, 0.5, 0.75, 1.0)
	
	out_color = vec4(0,.5,.75,1);
	//out_color = vec4(1,0,0,1);
}